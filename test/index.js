'use strict';

const assert = require('node:assert');
const { describe, it } = require('node:test');

const loader = require("../main");

describe('Loader', () => {
  describe('npm', () => {
    loader.npm();
  });

  describe('node', () => {

  });

  describe('module', () => {

  });

  describe('dir', () => {

  });
});